export enum OperatingSystem {
    ANDROID = "ANDROID",           // Consumer generated the click using the Android operating system.
    LINUX = "LINUX",               // Consumer generated the click using the Linux operating system.
    IOS = "IOS",                   // Consumer generated the click using the iOS operating system.
    WINDOWS_10 = "WINDOWS_10",     // Consumer generated the click using the Windows 10 operating system.
    WINDOWS_7 = "WINDOWS_7",       // Consumer generated the click using the Windows 7 operating system.
    UNKNOWN = "UNKNOWN"            // The OS could not be tracked.
  }
  